{
  "name": "encrypted-image-api",
  "version": "1.0.0",
  "description": "API for uploading and downloading encrypted images",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "generate": "prisma generate",
    "migrate": "prisma migrate dev --name init --preview-feature"
  },
  "dependencies": {
    "@prisma/client": "^3.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.17.1",
    "multer": "^1.4.2",
    "prisma": "^3.15.2",
    "swagger-ui-express": "^4.1.6",
    "yamljs": "^0.3.0"
  }
}
